<Window x:Class="ConditioningControlPanel.PinkRushPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="粉色冲刺！"
        Width="350" Height="150"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#E0151530" CornerRadius="12" BorderBrush="#FF1493" BorderThickness="2"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#FF1493" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 技能图片 -->
            <Border Grid.Column="0" Width="80" Height="80" Background="#1A1A2E" CornerRadius="8" Margin="0,0,15,0">
                <Image Source="pack://application:,,,/Resources/skills/pink_rush.png" Stretch="Uniform" Margin="5"/>
            </Border>

            <!-- 文本内容 -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="⚡ 粉色冲刺！" Foreground="#FF1493"
                           FontWeight="Bold" FontSize="18" Margin="0,0,0,4"/>
                <TextBlock Text="60 秒内 3 倍经验！" Foreground="#FFB6C1"
                           FontSize="13" Margin="0,0,0,6"/>
                <TextBlock x:Name="TxtCountdown" Text="剩余 60 秒" Foreground="#AAAAAA"
                           FontSize="12"/>
            </StackPanel>

            <!-- 关闭按钮 -->
            <Button Grid.Column="1" Content="✕"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Width="24" Height="24" Margin="0,-5,-5,0"
                    Background="Transparent" Foreground="#888888"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnClose_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
