<Window x:Class="ConditioningControlPanel.ContentModeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择模式"
        Height="420" Width="580"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True">

    <Border Background="#1A1A2E" CornerRadius="12" BorderBrush="#FF69B4" BorderThickness="2">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Grid.Row="0" Text="选择你的体验"
                       Foreground="#FF69B4" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <!-- 副标题 -->
            <TextBlock Grid.Row="1" Text="选择最适合你兴趣的内容模式，之后可以更改。"
                       Foreground="#B0B0B0" FontSize="13"
                       HorizontalAlignment="Center" Margin="0,0,0,25" TextWrapping="Wrap" TextAlignment="Center"/>

            <!-- 模式选择卡片 -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Bambi Sleep 卡片 -->
                <Border x:Name="CardBambi" Grid.Column="0" Background="#252542" CornerRadius="10"
                        Cursor="Hand" MouseLeftButtonDown="CardBambi_Click">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#404060"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#FF69B4"/>
                                    <Setter Property="Background" Value="#2A2A4A"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel VerticalAlignment="Center" Margin="20">
                        <TextBlock Text="BS" FontSize="36" FontWeight="Bold" Foreground="#FF69B4"
                                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Bambi Sleep" FontSize="18" FontWeight="SemiBold" Foreground="White"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock TextWrapping="Wrap" TextAlignment="Center" Foreground="#A0A0A0" FontSize="12" LineHeight="18">
                            <Run Text="Bambi Sleep 专属内容。"/>
                            <LineBreak/>
                            <Run Text="包含 BambiCloud 浏览器、"/>
                            <LineBreak/>
                            <Run Text="Bambi 主题触发器和提示。"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Sissy Hypno 卡片 -->
                <Border x:Name="CardSissy" Grid.Column="2" Background="#252542" CornerRadius="10"
                        Cursor="Hand" MouseLeftButtonDown="CardSissy_Click">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#404060"/>
                            <Setter Property="BorderThickness" Value="2"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#FF69B4"/>
                                    <Setter Property="Background" Value="#2A2A4A"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel VerticalAlignment="Center" Margin="20">
                        <TextBlock Text="SH" FontSize="36" FontWeight="Bold" Foreground="#FF69B4"
                                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Sissy Hypno" FontSize="18" FontWeight="SemiBold" Foreground="White"
                                   HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock TextWrapping="Wrap" TextAlignment="Center" Foreground="#A0A0A0" FontSize="12" LineHeight="18">
                            <Run Text="通用 sissy hypno 内容。"/>
                            <LineBreak/>
                            <Run Text="仅包含 HypnoTube 浏览器、"/>
                            <LineBreak/>
                            <Run Text="通用触发器和提示。"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
