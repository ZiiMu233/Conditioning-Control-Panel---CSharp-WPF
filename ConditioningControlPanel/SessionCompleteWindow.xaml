以下是汉化后的 `SessionCompleteWindow.xaml` 代码：

```xml
<Window x:Class="ConditioningControlPanel.SessionCompleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="会话完成"
        Width="500" Height="520"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">

    <Border Background="#1A1A2E" BorderBrush="#FF69B4" BorderThickness="3" CornerRadius="20" Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 卡片图片 -->
            <Border Grid.Row="0" CornerRadius="12" Margin="0,0,0,15" HorizontalAlignment="Center"
                    Background="#252540" Padding="5">
                <Image x:Name="ImgCard" Width="200" Height="140" Stretch="UniformToFill"
                       RenderOptions.BitmapScalingMode="HighQuality">
                    <Image.Clip>
                        <RectangleGeometry Rect="0,0,200,140" RadiusX="8" RadiusY="8"/>
                    </Image.Clip>
                </Image>
            </Border>
            
            <!-- 主要信息 -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                <TextBlock x:Name="TxtMainMessage" Text="好女孩！" 
                           Foreground="#FF69B4" FontSize="36" FontWeight="Bold" 
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="TxtSubMessage" Text="会话完成" 
                           Foreground="#808090" FontSize="16" 
                           HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- 统计 -->
            <Border Grid.Row="2" Background="#252540" CornerRadius="12" Padding="20" Margin="0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 会话名称 -->
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="会话" Foreground="#808090" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtSessionName" Text="清晨漂流" 
                                   Foreground="White" FontSize="14" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <!-- 时长 -->
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="时长" Foreground="#808090" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtDuration" Text="30:00" 
                                   Foreground="White" FontSize="14" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <!-- 获得经验 -->
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="获得经验" Foreground="#808090" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtXP" Text="+50" 
                                   Foreground="#90EE90" FontSize="18" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 关闭按钮 -->
            <Button Grid.Row="3" x:Name="BtnClose" Content="💗 继续" 
                    Background="#FF69B4" Foreground="White" 
                    BorderThickness="0" Padding="30,12" FontSize="16" FontWeight="Bold"
                    Cursor="Hand" Click="BtnClose_Click"
                    HorizontalAlignment="Center">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
```
