以下是汉化后的 `QuizReportWindow.xaml` 代码：

```xml
<Window x:Class="ConditioningControlPanel.QuizReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="测验报告"
        Width="700" Height="750"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Black"
        KeyDown="Window_KeyDown">

    <Grid>
        <!-- 背景 -->
        <Border>
            <Border.Background>
                <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.5,0.4" RadiusX="0.8" RadiusY="0.8">
                    <GradientStop Color="#1A0A2E" Offset="0"/>
                    <GradientStop Color="#0A0514" Offset="1"/>
                </RadialGradientBrush>
            </Border.Background>
        </Border>

        <!-- 暗角效果 -->
        <Border IsHitTestVisible="False">
            <Border.Background>
                <RadialGradientBrush Center="0.5,0.5" RadiusX="0.75" RadiusY="0.75">
                    <GradientStop Color="Transparent" Offset="0.5"/>
                    <GradientStop Color="#80000000" Offset="1"/>
                </RadialGradientBrush>
            </Border.Background>
        </Border>

        <Grid Margin="30,20,30,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 头部 -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,16">
                <TextBlock Text="测验报告" FontSize="28" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" Margin="0,0,0,4">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#FF69B4" BlurRadius="25" ShadowDepth="0" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock x:Name="TxtSubtitle" Foreground="#808090" FontSize="13"
                           HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock x:Name="TxtScore" FontSize="32" FontWeight="Bold" Foreground="#FF69B4"
                           HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#FF69B4" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>

            <!-- 滚动内容区 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12">
                <StackPanel x:Name="ContentPanel" MaxWidth="600" HorizontalAlignment="Center"/>
            </ScrollViewer>

            <!-- 关闭按钮 -->
            <Button Grid.Row="2" Content="关闭" Cursor="Hand" HorizontalAlignment="Center"
                    Background="#20FFFFFF" Foreground="#B0B0C8" FontWeight="Bold" FontSize="14"
                    BorderThickness="1" Padding="28,10" Click="BtnClose_Click">
                <Button.BorderBrush>
                    <SolidColorBrush Color="#40FFFFFF"/>
                </Button.BorderBrush>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>
```
